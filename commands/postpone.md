---
description: Postpone an active hydration reminder (+5 min, up to 2x before a hard lock)
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/*)
---

Run `${CLAUDE_PLUGIN_ROOT}/scripts/hydration-postpone.sh` and show its output to the user verbatim. Do not add commentary.
